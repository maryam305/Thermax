services:
  - type: web
    name: thermax-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd thermax_backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
